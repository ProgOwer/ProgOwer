image: registry.gitlab.com/pages/hugo/hugo_extended:latest

pages:
  script:
  - cd src
  - hugo --minify --gc -b https://progower.gitlab.io/progower/
  - mv public ../
  artifacts:
    paths:
    - public
  only:
  - master
